[project]
name = "weasyprint-server"
version = "0.0.1"
description = "html to pdf server"
readme = "README.md"
requires-python = "==3.12.4"

dependencies = [
  "weasyprint",
  "flask",
  "sentry-sdk",
  "pyuwsgi",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.6.2",
    "python-dotenv>=1.0.1",
]
